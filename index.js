//assumes line is left->right

module.exports = function (start, end) {
	var stepX = 1;
	var stepY = 1;
	var startCell = [Math.floor(start[0]), Math.floor(start[1])];
	var endCell = [Math.floor(end[0]), Math.floor(end[1])];

}